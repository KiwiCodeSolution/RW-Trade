import { Locale } from '@/types/baseTypes'

const SubcategoryWrapper = ({ slug, locale }: { slug: string; locale: Locale }) => {
	return <div>SubcategoryWrapper</div>
}
export default SubcategoryWrapper
